<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nibaron_kumar.classroutine20">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity01">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DayActivity" />
        <activity android:name=".routineActivity" />
        <activity android:name=".RoutineActivity" />
        <activity android:name=".B_Day" />
        <activity android:name=".C_Day" />
        <activity android:name=".D_Day" />
        <activity android:name=".E_Day"></activity>
    </application>

</manifest>